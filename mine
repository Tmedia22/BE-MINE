<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Something Magical ❤️</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

<div class="stars"></div>

<div class="container">
    <div class="card">
        <h1 id="typing"></h1>
        <p>You make my world brighter every single day ✨</p>

        <div class="buttons">

            <!-- Character Image -->
            <img src="character.png" class="character" alt="my character">
        
            <button class="yes" onclick="yesAnswer()">Yes 💖</button>
            <button class="no" id="noBtn">No 😅</button>
        
        </div>
        
        </div>
    </div>
</div>

<script>
// Typing Effect
const text = "Will you be my Valentine? ❤️";
let index = 0;
function typeEffect() {
    if (index < text.length) {
        document.getElementById("typing").innerHTML += text.charAt(index);
        index++;
        setTimeout(typeEffect, 80);
    }
}
typeEffect();

// Moving NO button
const noBtn = document.getElementById("noBtn");
noBtn.addEventListener("mouseover", function() {
    const x = Math.random() * (window.innerWidth - 100);
    const y = Math.random() * (window.innerHeight - 50);
    noBtn.style.position = "fixed";
    noBtn.style.left = x + "px";
    noBtn.style.top = y + "px";
});

// Floating Hearts
function createHeart() {
    const heart = document.createElement("div");
    heart.classList.add("heart");
    heart.innerHTML = "💖";
    heart.style.left = Math.random() * window.innerWidth + "px";
    heart.style.animationDuration = (3 + Math.random() * 3) + "s";
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}
setInterval(createHeart, 400);

// Confetti Explosion
function yesAnswer() {
    document.body.innerHTML = `
        <div class="celebration">
            <h1>She Said YES! 💍✨</h1>
            <p>This is the beginning of something beautiful ❤️</p>
        </div>
    `;

    for (let i = 0; i < 120; i++) {
        const confetti = document.createElement("div");
        confetti.classList.add("confetti");
        confetti.style.left = Math.random() * window.innerWidth + "px";
        confetti.style.backgroundColor = randomColor();
        confetti.style.animationDuration = (2 + Math.random() * 2) + "s";
        document.body.appendChild(confetti);
    }
}

function randomColor() {
    const colors = ["#ff4d6d", "#ffd166", "#06d6a0", "#118ab2", "#ffffff"];
    return colors[Math.floor(Math.random() * colors.length)];
}
</script>

</body>
</html>

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    height: 100vh;
    font-family: Arial, sans-serif;
    overflow: hidden;
    background: radial-gradient(circle at top, #ff758c, #2b1055);
    color: white;
}

/* Star background glow */
.stars {
    position: absolute;
    width: 100%;
    height: 100%;
    background: transparent;
    box-shadow: 
        50px 100px white,
        200px 300px white,
        400px 150px white,
        600px 400px white,
        800px 200px white;
    animation: twinkle 3s infinite alternate;
}

@keyframes twinkle {
    from { opacity: 0.3; }
    to { opacity: 1; }
}

/* Centered card */
.container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.card {
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(15px);
    padding: 40px;
    border-radius: 20px;
    text-align: center;
    width: 340px;
    box-shadow: 0 10px 40px rgba(0,0,0,0.3);
}

.card h1 {
    margin-bottom: 15px;
}

.card p {
    margin-bottom: 25px;
    font-size: 15px;
}

/* Buttons */
.buttons {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Character sitting on button */
.character {
    width: 50px;   /* adjust to your image */
    margin-bottom: -10px; /* makes it sit on the button */
    z-index: 2;
}


.yes {
    background: #ff4d6d;
    color: white;
}

.yes:hover {
    transform: scale(1.15);
    box-shadow: 0 0 20px #ff4d6d;
}

.no {
    background: #333;
    color: white;
}

/* Floating hearts */
.heart {
    position: absolute;
    bottom: -20px;
    font-size: 22px;
    animation: floatUp linear forwards;
}

@keyframes floatUp {
    from { transform: translateY(0); opacity: 1; }
    to { transform: translateY(-900px); opacity: 0; }
}

/* Celebration screen */
.celebration {
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    text-align: center;
}

/* Confetti */
.confetti {
    position: absolute;
    width: 8px;
    height: 15px;
    top: -10px;
    animation: fall linear forwards;
}

@keyframes fall {
    to {
        transform: translateY(100vh) rotate(720deg);
        opacity: 0;
    }
}
